{
  "name": "omni-extension-userdb",
  "packageManager": "yarn@3.3.1",
  "scripts": {
    "build": "esbuild --bundle ./extension.ts --outdir=../server  --format=esm --analyze --color=true --target=es2020 --platform=node --external:jsonata"
  },
  "devDependencies": {
    "esbuild": "^0.18.4",
    "typescript": "^5.1.3"
  }
}
